# ml/eval/

Evaluation — **the gate that authorizes the expensive EPYC build.**

**Status:** not started.

**Will contain:** the held-out eval set + the **RAG-vs-fine-tune comparison harness.** A pilot fine-tune must **beat a RAG baseline** here before any big hardware spend (and only when ambitions also exceed 24 GB VRAM). Treat as first-class infrastructure, not an afterthought.

**Learning-priority — TEACH** (root CLAUDE.md).
