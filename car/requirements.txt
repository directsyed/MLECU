# car/ecutune — offline deterministic ECU-tuning algorithm + safety layer.
# Separate venv from ml/data-pipeline (which stays stdlib-lean): the car layer needs
# numeric + property-testing deps. Conventions copied from corpus_pipeline, deps are not.
numpy>=2.0
pydantic>=2.7
pyyaml>=6.0
hypothesis>=6.100   # property-based fuzzing of the safety clamps (the provable bounds)
pytest>=8.0
