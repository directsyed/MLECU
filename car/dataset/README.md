# car/dataset/

The Subaru-first tuning corpus + archived real tuning iterations.

**Status:** empty (no logs yet — car domain is dormant).

**Will contain:** the **70% Subaru/EJ-platform / 30% general engine-management** split, and **archived tuning iterations (trims → change → result)** from Syed's own Stage-2/3 work — these are literally training examples in the exact form the model needs. Large/raw artifacts are gitignored.
