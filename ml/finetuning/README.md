# ml/finetuning/

QLoRA fine-tuning.

**Status:** not started; the pilot begins once ~2–5k curated pairs exist.

**Will contain:** QLoRA configs, dataset prep, training scripts/runs (pilot on **7B–14B** first). Model weights/checkpoints are gitignored, record results in `../../PROGRESS.md`.

**Learning-priority, TEACH** (root CLAUDE.md).
