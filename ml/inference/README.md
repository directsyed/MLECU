# ml/inference/

Model serving.

**Status:** not started (the GPU is up and idle, ready to host a first workload, likely the curation judge).

**Will contain:** serving config (llama.cpp / vLLM), quantization notes, and throughput/latency benchmarks (record the numbers in `../../PROGRESS.md`).

**Learning-priority, TEACH** (root CLAUDE.md).
